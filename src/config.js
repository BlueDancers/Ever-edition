let server = 'https://3nah32ea.qcloud.la'

let config = {
  server,                                 //请求前缀
  loginUrl:`${server}/weapp/login`,       //用户登录接口
  myColor: "#a1c4fd"
}

export default config;